<%= semantic_form_for @gallery do |f| %>
  <%= f.input :title %>
  <%= f.input :event, :label => t(:event) %>
  <%= f.input :description %>
  <%= f.submit create_or_update(@gallery), :class => :submit %>
<% end %>
